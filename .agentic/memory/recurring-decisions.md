# Recurring Decisions

Wiederkehrende Projektentscheidungen, die nicht jedes Mal neu diskutiert werden sollen.

- Beispiel: Wir verwenden `pnpm`, nicht `npm`.
- Beispiel: Neue API-Inputs werden zentral validiert.
