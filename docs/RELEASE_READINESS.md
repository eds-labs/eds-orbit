# Release readiness

2026-09-17 checkpoint: **locally tested application RC; not production/live accepted**. Production web build,253 application tests,12 framework tests and 4 browser workflows passed. The local production preview is available at localhost:4310. Fresh Linux ARM64 standalone and shared-host installations also passed, including HTTPS authentication, enforced resource limits, rendering and worker outage/recovery. No internal acceptance gate remains marked NOT_RUN.

## Completed local gates

Authenticated project roles, forced RLS, durable outbox/worker execution, source rights/revocation, hybrid retrieval mechanics, actual database-backed evaluation gates, finite planning, exact-package approvals, safe test publishing, operational visibility, responsive original-brand UI, data lifecycle, restore simulation and dependency/secret checks are executed. Full evidence and narrow result descriptions are in `ACCEPTANCE_REPORT.md` and the 88-row traceability matrix.

Defaults are Observe, test execution and external writes disabled. Empty production projects receive no hidden private/demo imports. Paid operations require a current price card, model capability configuration and explicit budget; old/unknown reservations remain counted. No model or index change auto-activates. Runtime cannot administer hosts or provider infrastructure.

## Required before production or public release

1. Review the recorded ARM64 image/container evidence against the chosen target architecture and workload. Measure target capacity and I/O behavior before production sizing; the bounded health probe does not establish sustained capacity. Build and validate AMD64 if that is the selected host architecture.
2. Choose a target host/domain and approve the concrete deployment, migration/rollback and single-writer cutover plan. Desk and Forecast remain separate. Do not reuse or replace existing host ports/DNS without that approval.
3. Configure application-owned OpenAI credentials/models/rates and approve a finite test budget. Execute genuine structured-output and >=60-case semantic/rights evaluation; preserve usage, cost and immutable index proof. Synthetic tests do not clear this gate.
4. Configure only intended provider accounts. Execute the explicit reviewed Postiz sandbox test (including PNG when needed), verify remote published account/ID, and test Slack delivery/owner response if enabled. Read-only Matomo reports require a scoped site token. Select the actual CMS/mail/ads providers before enabling those capabilities.
5. Configure off-host encrypted backups plus independent credential-key escrow, restore into a clean target, and test independent alarm delivery. Local restore/health checks do not prove off-host recoverability or remote notification.
6. Approve time-bounded project policies and mission actions. Verify quotas, quiet hours, spending limits and current public/model-use evidence. Reconcile pending/unknown remote actions before any publisher cutover.
7. Select/approve the code license and original-brand distribution rights; review operational evidence and generated assets before public repository publication. Remote CI has not been run because no repository was published/pushed.

No item above is silently authorized by passing a local build, entering credentials, or acknowledging this report. None was performed against production in this task. Tests and local fixes are authorized; external operational effects retain their explicit boundary.
